import { SlideEventsDirective } from './slide-events.directive';
import { ElementRef, inject } from '@angular/core';

describe('SlideEventsDirective', () => {
  it('should create an instance', () => {

    // inject([ElementRef], (elementRef: ElementRef) => {
    //   const directive = new SlideEventsDirective(elementRef);
    //   expect(directive).toBeTruthy();
    // });
    //
    // const directive = new SlideEventsDirective();
    // expect(directive).toBeTruthy();
  });
});
